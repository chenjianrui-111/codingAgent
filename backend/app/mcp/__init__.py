"""MCP (Model Context Protocol) server for dataToAi.

Exposes skills as MCP tools and datasets as MCP resources so that
MCP-compatible clients (Claude Desktop, Cursor, etc.) can discover
and invoke data analysis capabilities.
"""
